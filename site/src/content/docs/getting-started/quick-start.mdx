---
title: Quick Start
description: Pick your stack and get hook-sync running in 5 minutes. Go, Bun, Node, or any language with SQLite + HTTP.
---

import { Card, CardGrid } from '@astrojs/starlight/components';

Get a two-node active-active SQLite replication cluster running in 5 minutes.

## Pick Your Stack

Each runtime is self-contained — install, code, run, and topology links all in one place. All three sync to each other transparently. Mix and match freely.

<CardGrid stagger>
  <Card title="Go" icon="seti:go" href="/runtimes/go/">
    `go get` + importable packages. Trigger and hook capture modes. Pre-built binaries.
  </Card>
  <Card title="Bun" icon="bun" href="/runtimes/bun/">
    `bun add hooksync.js` + `bun:sqlite`. Trigger-based capture. `Bun.serve()` wiring.
  </Card>
  <Card title="Node.js" icon="node" href="/runtimes/node/">
    `npm install hooksync.js` + `better-sqlite3`. Trigger-based capture. `http.createServer` or Express.
  </Card>
  <Card title="Other Languages" icon="seti:custom" href="/runtimes/implement/">
    Rust, Python, PHP, or any language with SQLite + HTTP. Wire protocol is language-agnostic.
  </Card>
</CardGrid>

## Table Requirements

Every synced table **must** have:

- `id` — `TEXT PRIMARY KEY` (UUID, zero conflict)
- `updated_at` — `INTEGER` (millisecond timestamp, for last-write-wins)

```sql
CREATE TABLE items(
  id TEXT PRIMARY KEY,
  name TEXT,
  value INTEGER,
  created_at INTEGER,
  updated_at INTEGER
);
```

## What Happens Automatically

When you call `attach()` (JS) or `trigger.Attach()` (Go), the library:

1. Creates `_meta`, `_changes`, `_dead_letter`, `_peer_state` tables
2. Auto-generates INSERT/UPDATE/DELETE triggers via schema introspection
3. Starts a background ship loop (50ms default)
4. Handles ACK-based retry with exponential backoff
5. Manages per-peer watermarks for multi-peer topologies

You only write CRUD endpoints + wire `POST /sync` to `mgr.applyChanges()` (JS) or `http.Handle("/sync", mgr)` (Go).

## Next Steps

Once you have two nodes syncing, explore topologies:

- [Point-to-Point](/topologies/point-to-point/) — 2 nodes, active-active
- [Full Mesh](/topologies/full-mesh/) — 3-7 nodes, all-to-all
- [Dedicated Hub](/topologies/hub/) — 8+ nodes, Go-only relay
- [Multi-Region](/topologies/multi-region/) — Hub-to-hub cross-region
